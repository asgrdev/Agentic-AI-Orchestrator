# ═══════════════════════════════════════════════════════════════════
# Agentic Graph RAG — نیازمندی‌های کامل پروژه
#
# نصب:            pip install -r requirements.txt
# پایتون:         3.11+ (تست‌شده روی 3.13)
# توجه: Weaviate به‌صورت سرویس جدا اجرا می‌شود (docker) — پایین را ببینید
# ═══════════════════════════════════════════════════════════════════

# ── هسته ─────────────────────────────────────────────────────────────
numpy>=1.26
pandas>=2.1
pydantic>=2.5
httpx>=0.27
requests>=2.31
psutil>=5.9
loguru>=0.7
rich>=13.7
python-dotenv>=1.0

# ── LLM / مدل‌ها ─────────────────────────────────────────────────────
torch>=2.3
torchaudio>=2.3
transformers>=4.44
tiktoken>=0.7
# MLX فقط روی Apple Silicon (phi3 / granite)
mlx>=0.21;    sys_platform == "darwin" and platform_machine == "arm64"
mlx-lm>=0.20; sys_platform == "darwin" and platform_machine == "arm64"
# backend های جایگزین LLM (در صورت نیاز فعال کنید):
# llama-cpp-python>=0.3     # اجرای GGUF (Phi-3-mini-4k-instruct-q4)
# ollama>=0.3               # کلاینت Ollama

# ── Vector Store / Graph DB / Search ────────────────────────────────
weaviate-client>=4.7
kuzu>=0.6
opensearch-py>=2.6

# ── NLP / NER / استخراج موجودیت ─────────────────────────────────────
spacy>=3.7
gliner>=0.2
# مدل spacy از فایل لوکال پروژه:
# pip install models/en_core_web_trf-any-py3-none-any.whl

# ── Vision / Audio (skills سنسور) ───────────────────────────────────
ultralytics>=8.3          # YOLO11 (detect / pose / segment)
opencv-python>=4.9
openai-whisper>=20231117  # گفتار به متن (whisper-medium لوکال)
sounddevice>=0.4
soundfile>=0.12
Pillow>=10.2

# ── پردازش سند ──────────────────────────────────────────────────────
docling>=2.0              # استخراج پیشرفته PDF
PyMuPDF>=1.24             # fitz — خواندن PDF (fallback)
python-docx>=1.1
openpyxl>=3.1             # pandas.read_excel
pytesseract>=0.3          # OCR (نیازمند نصب سیستمی tesseract)

# ── جمع‌آوری داده‌ی خارجی (data_collector) ───────────────────────────
wikipedia-api>=0.6
arxiv>=2.1
pyalex>=0.14              # OpenAlex
semanticscholar>=0.8
feedparser>=6.0           # RSS
newspaper4k>=0.9          # استخراج متن خبر
beautifulsoup4>=4.12
praw>=7.7                 # Reddit
yfinance>=0.2
fredapi>=0.5
wbdata>=1.0               # World Bank
datasets>=2.19            # HuggingFace datasets

# ── UI / API ─────────────────────────────────────────────────────────
gradio>=5.0               # حباب «فرایند فکر» نیازمند gradio>=5 است
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
python-multipart>=0.0.9
aiohttp>=3.9

# ── تست / توسعه ─────────────────────────────────────────────────────
pytest>=8.0

# ── اختیاری (production / monitoring) ───────────────────────────────
# gunicorn>=21.2
# prometheus-client>=0.19
# cloudscraper>=1.2        # دور زدن Cloudflare در newspaper

# ═══════════════════════════════════════════════════════════════════
# سرویس‌های جانبی (خارج از pip):
#   Weaviate:   docker run -d -p 8080:8080 -p 50051:50051 \
#                 cr.weaviate.io/semitechnologies/weaviate:1.27.0
#   Tesseract:  brew install tesseract        (برای pytesseract)
#   FFmpeg:     brew install ffmpeg           (برای whisper)
#
# مدل‌های لوکال در پوشه‌ی models/ قرار می‌گیرند — configs/model_catalog.py
# ═══════════════════════════════════════════════════════════════════
